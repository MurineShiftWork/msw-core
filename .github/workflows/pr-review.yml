name: PR Review

on:
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  review:
    name: Review
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "PR review placeholder"
